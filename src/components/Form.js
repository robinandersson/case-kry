import React, { useState } from 'react';

const Form = props => {
  return (
    <form className="w-full max-w-lg bg-white shadow-lg mx-auto p-10 rounded-xl">
      <button type="submit" className="btn">
        Next
      </button>
    </form>
  );
};

export default Form;
